[versions]
# Update Kotlin to a version that supports the Compose plugin, for example, 2.0.0 or higher
kotlin = "2.0.0"
# The Compose Compiler version will now be tied to the Kotlin version
agp = "8.13.1"
coreKtx = "1.12.0"
lifecycleRuntime = "2.7.0"
activityCompose = "1.8.2"
glance = "1.0.0"
workManager = "2.9.0"
dataStore = "1.0.0"
junit = "4.13.2"

[libraries]
# ... (no changes needed here)

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
# Correctly define the Compose Compiler plugin, tying its version to the Kotlin version
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }

[bundles]
# ... (no changes needed here)
